import 'package:flutter/material.dart';

class GameTileEntity {
  int index;
  String title;
  String description;
  Color color;

  GameTileEntity(
      {required this.index,
      required this.title,
      required this.description,
      required this.color});
}
